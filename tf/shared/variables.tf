variable aws_region {
    type = string
    description = "The AWS region."
}

variable aws_profile {
    type = string
    description = "The AWS profile to authenticate with. Usually stored in the /Users/<USER>/.aws/"
}